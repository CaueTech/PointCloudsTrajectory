# PointCloudsTrajectory
This project implements a Python algorithm that estimates the trajectory of an object from a dataset of 30 point cloud scans. The code leverages external libraries such as NumPy for numerical computations and Matplotlib for visualization.
